# Storybook 8.6 的 peerDependencies 尚未声明 React 19 支持，
# 实际运行时兼容。用 legacy-peer-deps 跳过 npm 的 peer 冲突校验。
legacy-peer-deps=true
# react/react-dom 在 devDependencies 中（peer 双声明），确保本地开发能装上，
# 覆盖全局 npm 配置可能的 omit=dev。
include=dev
